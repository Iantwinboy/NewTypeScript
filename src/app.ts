const myString: string = 'Hello, world!'; // create a new string variable
const appDiv = document.createElement('div'); // create a new div element
appDiv.innerText = myString; // set the text content of the div element to the string value
document.body.appendChild(appDiv); // add the div element to the HTML document
